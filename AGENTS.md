# 開発上の規則

- ユーザー向けの説明、工程名、担当別プロンプトは日本語で記述する。
- `docs/roadmap.md` を変更しない。
- 認証情報、取得データ、実行結果、`.harness/` をGitへ追加しない。
- ハーネスの工程では、実装と修正を `gpt-5.6-sol` medium、テストを `gpt-5.6-sol` medium、レビューを `gpt-6-astra` highで実行する。モデルを自動代替しない。
- テスト担当とレビュー担当は製品コードを変更しない。
- 発注先（Codex）へ渡すものは `.codex/` に、発注元（Claude）が持つ制御・検証・検収は `.claude/` に置く。両者を混ぜない。
- 反映前の最終レビュー（検収）は `claude-fable-5-1` がClaude Codeセッションで行う。ハーネスはClaudeを子プロセスとして起動しない。
- 最終レビュー担当も製品コードと作業コピーを変更しない。指摘には必ず実行証跡のパスを付ける。
- ハーネス本体（`.claude/` `.codex/` とそのテスト）はClaude Codeが実装する。Codexが担うのは、ハーネスが駆動する測定側の作業である。
- commitとpushはユーザーから個別の指示がない限り行わない。

