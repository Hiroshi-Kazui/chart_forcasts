# 開発ハーネス独立レビュー

2026-09-21。担当: gpt-6-astra / high。実装・修正を担当したエージェントとは独立に、製品コード、回帰試験コード、実モデルの保存証跡を確認した。

## 判定

**PASS。残存するP0〜P2の必須修正はない。**

最終製品ディレクトリの識別hash（独立試験担当の記録）:
`988114411417b774b2cd21742b2da2e6bbbb005416a9681f5bfbd4ba99a25757`

この判定は、下記の実モデル実行版と、その後に追加された共通プロンプトの文言を区別している。最終版と完全に同じバイト列で実モデル試験を再実行した、という意味ではない。

## 実モデル証跡の独立照合

対象runは `20260921-224957-e7b9a03f`。証跡は
`C:/develop/chart_forecasts_harness_fix_trial/.harness/runs/20260921-224957-e7b9a03f/` に保存されている。

| 工程 | 指定モデル・推論強度 | 保存結果 |
|---|---|---|
| 01 実装 | gpt-5.6-sol / medium | 検収用の `a - b` を作成、PASS |
| 02 テスト | gpt-5.6-sol / medium | 固定argvの実行がexit 1、AssertionError、担当結果もFAIL |
| 03 修正 | gpt-5.6-sol / medium | 独立したFIXで `a + b` に修正、PASS |
| 04 再テスト | gpt-5.6-sol / medium | 同じ固定argvがexit 0、担当結果PASS |
| 05 レビュー | gpt-6-astra / high | PASS、review_findingsは空 |

各工程の `invocation.json`、`process.json`、`result.json` と検証実行器の `verification.json` を直接読んだ。全工程に独立した起動証跡があり、指定モデルの代替はない。エージェントプロセスのexit 0だけで成功と判断していない。

`03-修正/prompt.txt` には、独立テストの失敗報告に加え、真正な検証結果のargv、exit 1、AssertionError、証跡パスが渡されている。`control-decisions.jsonl` でも初回FAILと再テストPASSを追跡できる。

再テストの開始・終了コードhash、`tested-code-hash.txt`、現在の作業コピーから独立に再計算したhashは、すべて次の値に一致した。

`0b7030dc3c2dbcf36b88e8c7b13177520a9065b684566196ddf5fbd0dc9ae0d7`

最終状態はCOMPLETED、fix_countは1、`changes.json` の反映対象は `calc.py` のみ。反映先と作業コピーの `calc.py` のSHA-256も、ともに
`ba1a531f581d2e6094e978ed6f7aca7a8d92eeb62c6e7ad73ee692f7f18bc772`
で一致した。

先行する未達runは成功実績に含めていない。実装工程内の自己修正を独立FIXと誤認せず、後のrunで実際の独立FIXを確認した。

## 実モデル実行版と最終版の差

保存されたruntimeと現在の `dev_harness` の全ソースをhash比較し、異なるファイルは `controller.py` だけであることを確認した。さらに差分を読み、変更が次の共通プロンプト1文の追加のみであることを確認した。

> 要件、受入条件、必要入力に不明点や矛盾があれば推測で補わず、statusをBLOCKEDにして不足内容をissuesへ記録してください。

状態遷移、検証処理、プロセス制御、反映処理に差分はない。進行中の固定runtimeは変更していない。この文言追加後は、独立試験担当による実子プロセスを起動しない短回帰3件とruffが合格した。文言差分は静的に確認し、実モデルの再実行は行っていない。

## 修正を確認した必須指摘

| ID | 確認内容 |
|---|---|
| R1 | Windowsプロセスをsuspendedで生成し、Jobへ登録後に再開。割当失敗時も未実行の子を終了 |
| R2 | 工程、指摘、累積消費時間、開始時刻を保存し、複数回の再開・異常終了で予算を戻さない |
| R3 | 制御プロセスの異常終了と起動失敗による残留状態を照合・回復 |
| R4 | FIX上限後の無料再実装を拒否し、既存FIXや反映直前の明示停止は安全に再開 |
| R5 | 開始時の要件原文を固定し、変更を拒否。レビューへ原本・作業コピー・証跡を提示 |
| R6 | Windowsのrooted/drive-relativeパスを拒否。junction検出を備えるPython 3.12以上を対応版として明示 |
| R7 | HTTP成功と検証成功を分離し、検証失敗時のclientは実結果を表示してexit 1を返す |
| R8 | FIXとレビューへ具体的な検証失敗と制御判断履歴を引き継ぐ |

Windows非表示起動についても、製品・試験の生成経路を静的に確認した。独立試験担当の短時間実測では、子のGetConsoleWindow()は0、前後のforeground window handleは同じだった。共通プロンプトにも補助プロセスの非表示起動を指示している。

独立試験の結果は46 passed、1 skipped、ruff合格。symlinkの動的試験1件はWindowsの作成権限不足でskipされている。このskipを実行合格とは扱わず、リンク拒否のコードを静的に確認した。詳細は [独立試験報告](dev-harness-test-report.md) を参照。

## P3助言

`agent.py` の `.cmd` / `.bat` 入口については、空白やcmdのメタ文字を含む作業ディレクトリ・入口パスの回帰を将来追加するとよい。通常のWindowsパスでの実モデル起動は今回確認済みだが、その特殊なパス条件までは検収していない。今回の合格を妨げる必須指摘にはしていない。

参照測定タスクの具体値は、親の管理担当が既存の合意済み計画と照合した。今回行ったのはハーネス検収であり、参照測定タスクの実装・測定、GitHub実行、commit、pushは行っていない。
